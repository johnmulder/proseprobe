# Sequential step prose

First, install the signed client package on the test host. Second, select the checked configuration file for that host. Third, run the validation command and inspect its recorded result.

1. First, install the signed client package on the test host.
2. Second, select the checked configuration file for that host.
3. Third, run the validation command and inspect its recorded result.

First, install the client package on the test host. Second, select its configuration file.

First, the operator installs the client package on the test host.

Second, the operator selects the checked configuration file for that host.

Third, the operator runs the validation command and inspects its recorded result.

First, install the client package. Third, run the validation command. Second, select the configuration file.

First attempt records the baseline. Second, compare the new result. Third, publish the report.
