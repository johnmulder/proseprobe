# Imprecise-quantity checks

A large number of requests failed during the deployment window.

The queue contains a small number of delayed jobs.

A considerable number of clients still use the legacy endpoint.

The incident affected a handful of worker processes.

## Supported claims

The benchmark found a large number of failures across 500 requests.

In July, Mina Ortiz reported a handful of retries in the survey.

## Exclusions

### A large number of failures

Use `a small number of` as the legacy label in this guide.

```text
a handful of failures
```

## Example

A considerable number of clients retry in this demonstration.

## Near misses

Many clients sent some requests, and several retries succeeded.

The measured count was 42 requests, which is already precise.
