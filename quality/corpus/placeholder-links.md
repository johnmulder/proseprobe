# Placeholder link review

Replace [the runbook](URL_HERE) before publishing.
Set [the fallback][fallback] before publishing.
[fallback]: INSERT_URL

Review [the empty target]() before publishing.
Review [the top target](#) before publishing.

Use [the guide](/guide), [the section](#install), [email](mailto:ops@example.com), and [the example](https://example.com/docs).
