# Empty-It opener checks

It is clear that the cache is stale.

It is obvious that the retry failed.

It is evident
that the timeout is repeatable.

## Example
It is clear that this sentence quotes the phrase.

## Findings

Use `It is obvious that` only when quoting legacy prose.

```text
It is evident that
```

It is possible that the cache is stale.
