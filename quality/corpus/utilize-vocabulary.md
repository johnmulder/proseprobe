# Verb choice

The adapter will utilize the system parser for each input file.

The legacy wrapper utilised the shared parser during migration.

The report records CPU utilization for each request.

Use `utilize` only when quoting the legacy command name.

The utility process loads the parser before validation begins.
