# Hedged-requirement checks

The service must normally reject an unsigned archive.

Operators should generally rotate the token after each release.

> Clients MUST NORMALLY provide a signature.

## Close non-matches and excluded contexts

The service must reject an unsigned archive.

The service normally rejects an unsigned archive.

Operators should rotate the token after each release.

Operators generally rotate the token after each release.

The service must not normally reject an unsigned archive.

In this standard, "must normally" means required in ordinary circumstances.

The phrase "should generally" is defined as advisory guidance.

In this standard, **must normally** refers to an ordinary requirement.

## The service must normally reject unsigned input

Use `should generally` as sample text.

```text
The service must normally reject unsigned input.
```

## Example

Operators should generally rotate the token.
