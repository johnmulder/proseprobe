# Double-negative checks

The outcome is not uncommon.
A retry is not unlikely.
Recovery is not impossible.

Use `not uncommon` when quoting the legacy phrase.

```text
not unlikely
```

The outcome is not common, and recovery is possible.
