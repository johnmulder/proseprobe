# Ambiguous-This opener checks

This causes one retry after a timeout.

This means the cache remains stale after restart.

> This shows the timeout repeats under load.

## Supported contexts

This cache causes every request to miss.

This caused the retry storm during deployment.

This indicates that the cache remains stale after restart.

That shows the timeout remains repeatable under load.

The report says this shows the timeout repeats under load.

This means-tested benefit remains available to eligible users.

## This shows the timeout repeats under load

Use `This means the cache is stale` as sample text.

```text
This causes every request to fail.
```

## Example

This shows the timeout repeats under load.
