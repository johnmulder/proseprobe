# Former/latter reference checks

Choose the local or remote runner; the former writes to disk.

The file and database backends differ; the latter records a version.

> Compare both formats; THE LATTER uses fewer bytes.

## Close non-matches and excluded contexts

A former release used a different checksum.

The formerly active node now rejects writes.

The lattermost stage emits the archive.

The first backend writes to disk; the second records a version.

## The former writes to disk

Use `the latter` as sample text.

```text
The former accepts writes.
```

## Example

The latter needs a token.
