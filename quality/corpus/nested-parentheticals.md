# Nested-parenthetical checks

Configure the cache (for example (on Linux)) before startup.
The retry policy (including defaults (100 ms and three attempts)) is explicit.
The parser (supports a nested
(parenthetical note
across source lines) in comments).

Use `outer(inner(value))` in the legacy example.

[Read the reference](https://example.com/(v1)).

```text
(outer (inner))
```

One parenthetical (with one level) is acceptable.
