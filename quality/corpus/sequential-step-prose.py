"""Sequential step prose examples."""


def _deploy():
    """First, install the client. Second, configure it. Third, validate it."""
    return None


def _partial_sequence():
    """First, install the client. Second, configure it."""
    return None


def _separated_sequence():
    """First, install the client.

    Second, configure it.

    Third, validate it.
    """
    return None


def _reordered_sequence():
    """First, install the client. Third, validate it. Second, configure it."""
    return None


def _ordinal_noun():
    """First attempt records the baseline. Second, compare it. Third, publish it."""
    return None
