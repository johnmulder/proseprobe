# Existential-opener checks

There is one retry path after each failed request.

There are two retry queues for failed requests.

> There was one stale cache entry after deployment.

- There were several timeout reports after region recovery.

## Supported contexts

There is no cache configured here.

There are only two supported choices.

There will be another retry path after failed requests.

There has been another retry path after failed requests.

The report says there are several retry paths after failed requests.

## There are several retry paths after failed requests

Use `There is one retry path after failed requests` as sample text.

```text
There are several retry paths after failed requests.
```

## Example

There was one stale cache entry after deployment.
