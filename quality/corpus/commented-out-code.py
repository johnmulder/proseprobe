"""Commented-out code checks."""

# cache = load_cache()
# send(payload)
# from pathlib import Path
# if ready:

value = 1  # cache = load_cache()

# Use send(payload) to notify the client.
# TODO: Remove after issue 42
# Note: caller owns retries
# value: int
# first = 1; second = 2
# def build():
# return result
# [item for item in items]
# >>> send(payload)
# if ready, send payload.
# try:

message = "# cache = load_cache()"
