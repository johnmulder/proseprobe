# ALL-CAPS emphasis checks

The recovery guide says DO NOT DELETE THIS FILE during an active restore.

Operators SHOULD NEVER IGNORE THIS WARNING during checksum repair.

- The prompt says YOU MUST ALWAYS VERIFY the archive checksum.

## Close non-matches and excluded contexts

Use the HTTP GET API for status checks.

The API and CLI share JSON output.

Set MAX_RETRY_COUNT before the worker starts.

The client MUST NOT retry failed requests.

Use POST AND DELETE methods for request routing.

The schema calls it USER ACCESS LEVEL.

> You MUST NEVER DELETE local state.

## A heading with DO NOT DELETE THIS text

Use `MUST NOT DELETE` as sample text.

[Reference](DO NOT DELETE THIS) remains unresolved.

```text
MUST NOT DELETE
```

## Example

You MUST NEVER DELETE local state.
